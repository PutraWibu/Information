<?php
session_start();

// Password sederhana untuk demo
$admin_password = "admin123";
$is_admin = isset($_SESSION['logged_in']) && $_SESSION['logged_in'] === true;

// Logic Logout
if (isset($_GET['logout'])) {
    session_destroy();
    header("Location: " . $_SERVER['PHP_SELF']);
    exit;
}

// Logic Login
$error = "";
if (isset($_POST['login'])) {
    if ($_POST['password'] === $admin_password) {
        $_SESSION['logged_in'] = true;
        header("Location: " . $_SERVER['PHP_SELF']);
        exit;
    } else {
        $error = "Password salah!";
    }
}
?>
<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PutraMods Informations | Official Portal</title>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;600;800&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        :root {
            --bg: #000000;
            --surface: #111111;
            --border: #222222;
            --accent: #ffffff;
            --text-p: #999999;
            --text-h: #ffffff;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Plus Jakarta Sans', sans-serif; }

        body { background: var(--bg); color: var(--text-h); line-height: 1.7; }

        /* Navigation */
        nav {
            padding: 1.5rem 8%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid var(--border);
            position: sticky;
            top: 0;
            background: rgba(0,0,0,0.8);
            backdrop-filter: blur(12px);
            z-index: 100;
        }

        .logo { font-weight: 800; font-size: 1.4rem; letter-spacing: -0.5px; }
        .logo span { color: var(--text-p); font-weight: 400; }

        .admin-trigger {
            background: var(--accent);
            color: var(--bg);
            padding: 8px 16px;
            border-radius: 8px;
            font-weight: 600;
            font-size: 14px;
            cursor: pointer;
            border: none;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        /* Main Post Area */
        .container { max-width: 900px; margin: 60px auto; padding: 0 20px; }

        .post-header { margin-bottom: 40px; text-align: center; }
        .category-tag { 
            display: inline-block;
            padding: 4px 12px;
            border: 1px solid var(--border);
            border-radius: 20px;
            font-size: 12px;
            text-transform: uppercase;
            margin-bottom: 15px;
            color: var(--text-p);
        }

        .post-title { font-size: clamp(2rem, 5vw, 3.5rem); line-height: 1.1; margin-bottom: 20px; letter-spacing: -2px; }

        .post-main-card {
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: 24px;
            padding: 40px;
            position: relative;
            overflow: hidden;
        }

        .post-main-card::before {
            content: "";
            position: absolute;
            top: 0; left: 0; width: 100%; height: 4px;
            background: linear-gradient(90deg, #333, #fff, #333);
        }

        .post-content {
            font-size: 1.15rem;
            color: #ccc;
            white-space: pre-line; /* Menjaga format teks */
        }

        /* Admin UI */
        .modal {
            display: none;
            position: fixed;
            inset: 0;
            background: rgba(0,0,0,0.95);
            z-index: 1000;
            align-items: center;
            justify-content: center;
        }

        .modal-box {
            background: #111;
            border: 1px solid var(--border);
            padding: 40px;
            border-radius: 20px;
            width: 100%;
            max-width: 450px;
        }

        input {
            width: 100%;
            background: #000;
            border: 1px solid var(--border);
            padding: 15px;
            color: #fff;
            margin: 20px 0;
            border-radius: 10px;
        }

        .btn-black {
            width: 100%;
            padding: 15px;
            background: #fff;
            color: #000;
            border: none;
            border-radius: 10px;
            font-weight: 700;
            cursor: pointer;
        }

        .admin-dashboard { text-align: center; }
        .stats-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin: 20px 0; }
        .stat-item { background: #000; padding: 15px; border-radius: 10px; border: 1px solid var(--border); }

        footer { border-top: 1px solid var(--border); padding: 40px; text-align: center; color: var(--text-p); font-size: 14px; margin-top: 80px; }

        @media (max-width: 600px) {
            .post-main-card { padding: 25px; }
        }
    </style>
</head>
<body>

    <nav>
        <div class="logo">PutraMods <span>Informations</span></div>
        <button class="admin-trigger" onclick="toggleAdmin()">
            <i data-lucide="shield-check" size="18"></i> 
            <?php echo $is_admin ? 'Dashboard' : 'Admin Area'; ?>
        </button>
    </nav>

    <div class="container">
        <header class="post-header">
            <span class="category-tag">Featured Update</span>
            <h1 class="post-title">PutraMods Informations</h1>
        </header>

        <article class="post-main-card">
            <div class="post-content">
                <!-- TEMPAT TEKS ANDA DI BAWAH INI -->
                PutraMods Informations hadir sebagai wadah informasi digital yang modern dan efisien. 
                Kami berkomitmen untuk menyajikan konten berkualitas dengan tampilan yang nyaman di mata. 
                
                Gunakan panel admin untuk mengelola konten Anda secara mandiri. Desain ini telah dioptimalkan 
                untuk kecepatan akses dan responsivitas di berbagai perangkat, baik ponsel maupun desktop.
                
                Dengan integrasi sistem satu file (PHP), manajemen situs menjadi jauh lebih praktis tanpa mengorbankan estetika profesionalisme.
            </div>
        </article>
    </div>

    <!-- Admin Panel -->
    <div class="modal" id="adminModal">
        <div class="modal-box">
            <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:20px">
                <h2 style="font-size:1.5rem">System Control</h2>
                <button onclick="toggleAdmin()" style="background:none; border:none; color:white; cursor:pointer"><i data-lucide="x"></i></button>
            </div>

            <?php if (!$is_admin): ?>
                <form method="POST">
                    <p style="color:var(--text-p)">Masukkan akses enkripsi untuk masuk ke mode pengembang.</p>
                    <input type="password" name="password" placeholder="Key Password" required autofocus>
                    <?php if($error) echo "<p style='color:#ff4444; margin-bottom:15px; font-size:14px'>$error</p>"; ?>
                    <button type="submit" name="login" class="btn-black">Otorisasi</button>
                </form>
            <?php else: ?>
                <div class="admin-dashboard">
                    <div class="stats-grid">
                        <div class="stat-item">
                            <small style="color:var(--text-p)">Visitor</small>
                            <div style="font-size:1.2rem; font-weight:800">1,240</div>
                        </div>
                        <div class="stat-item">
                            <small style="color:var(--text-p)">Posts</small>
                            <div style="font-size:1.2rem; font-weight:800">12</div>
                        </div>
                    </div>
                    <button class="btn-black" style="background:#222; color:#fff; border:1px solid #333; margin-bottom:10px">Edit Postingan</button>
                    <button class="btn-black" style="background:#222; color:#fff; border:1px solid #333; margin-bottom:20px">Pengaturan Situs</button>
                    <a href="?logout=1" style="color:#ff4444; text-decoration:none; font-size:14px; font-weight:600">Log Out dari Sesi</a>
                </div>
            <?php endif; ?>
        </div>
    </div>

    <footer>
        <p>&copy; <?php echo date('Y'); ?> PutraMods Informations. Minimalist & Powerfull.</p>
    </footer>

    <script>
        lucide.createIcons();

        function toggleAdmin() {
            const modal = document.getElementById('adminModal');
            modal.style.display = (modal.style.display === 'flex') ? 'none' : 'flex';
        }

        // Close on Esc
        document.onkeydown = function(evt) {
            evt = evt || window.event;
            if (evt.keyCode == 27) toggleAdmin();
        };
    </script>
</body>
</html>
